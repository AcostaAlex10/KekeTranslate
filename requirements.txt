# --- Backend ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.13.5            # 2.13 es la que exige google-genai
pydantic-settings==2.7.1
httpx==0.28.1
aiofiles==24.1.0

# --- Proveedores de IA ---
anthropic>=1.0.0            # Anotador IA (Claude)
google-genai>=2.20.0        # Anotador IA (Gemini): alternativa con nivel gratuito

# --- Cuentas ---
argon2-cffi>=23.1           # Hash de contrasenas (Argon2id, lo que recomienda OWASP)

# --- Material de la biblioteca ---
pypdf>=5.1                  # Extraer el texto de los PDF para que la IA los lea

# --- Frontend ---
streamlit==1.42.2           # 1.42 es la primera con `st.context.cookies`

# No se sube mas alla de 1.42.x mientras el backend siga en FastAPI 0.115: a
# partir de 1.43 Streamlit exige starlette >= 0.46 y FastAPI 0.115 exige < 0.42.
# Se probo 1.63 y rompe el backend entero. Subir mas obliga a subir FastAPI
# tambien, y eso es otra tanda con su propia verificacion.

# --- Utilidades / tests ---
python-dotenv==1.0.1
truststore>=0.10            # Confiar en los certificados del sistema (antivirus/proxy)
pytest==8.3.4
pytest-asyncio==0.25.2
